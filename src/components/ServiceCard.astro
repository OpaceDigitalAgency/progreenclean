---
interface Props {
  title: string;
  slug: string;
  icon: string;
  description: string;
  price: string;
}

const { title, slug, icon, description, price } = Astro.props;
---
<a href={`/services/${slug}/`} class="group block bg-card-bg rounded-2xl p-6 hover:shadow-lg transition-all duration-300 hover:border-primary/20 hover:-translate-y-1">
  <div class="w-12 h-12 rounded-full bg-primary flex items-center justify-center mb-4 group-hover:bg-primary-dark transition-colors">
    <span class="material-symbols-rounded text-2xl text-white">{icon}</span>
  </div>
  <h3 class="font-heading font-bold text-lg text-navy mb-2 group-hover:text-primary transition-colors">{title}</h3>
  <p class="text-navy/60 text-sm leading-relaxed mb-3">{description}</p>
  <span class="text-primary font-heading font-bold text-sm">{price} →</span>
</a>

