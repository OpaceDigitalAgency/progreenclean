---
const trustItems = [
  'Reliability and Quality',
  'Eco-Friendly Products',
  'Fully Insured & DBS Checked',
  'Local Epsom, Surrey Team',
  'Flexible Scheduling',
  'Satisfaction Guaranteed',
];
---
<section class="bg-white py-10 border-y border-gray-100">
  <div class="max-w-7xl mx-auto px-4 lg:px-8">
    <h2 class="font-heading font-bold text-2xl text-navy text-center mb-8">Why Choose Us</h2>
    <div class="flex flex-wrap justify-center gap-x-8 gap-y-4">
      {trustItems.map((item) => (
        <div class="flex items-center gap-2">
          <div class="w-8 h-8 rounded-full bg-accent flex items-center justify-center flex-shrink-0">
            <svg class="w-4 h-4 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" /></svg>
          </div>
          <span class="font-heading font-semibold text-sm text-navy">{item}</span>
        </div>
      ))}
    </div>
  </div>
</section>

